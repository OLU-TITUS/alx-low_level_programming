#include "main.h"

/**
 * _abs - Check absolute
 *  @b: an integer
 *  Description: This script return absolute value of a number
 *  Return: Absolute value of b
 */

int _abs(int b)
{
	if (b >= 0)
		return (b);
	else
		return (b * -1);
}
